{
  "name": "geond-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Local agent workbench for coding agents, sessions, diffs, and tool orchestration.",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @geond-agent/desktop dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
