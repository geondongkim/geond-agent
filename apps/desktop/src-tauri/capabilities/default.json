{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the geond-agent first slice.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
