{
  "name": "@geond-agent/backend-adapter-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Backend adapter contracts, capabilities, and normalized event types for geond-agent.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
