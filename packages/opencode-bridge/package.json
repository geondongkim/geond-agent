{
  "name": "@geond-agent/opencode-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "Metadata-only OpenCode adapter research boundary for geond-agent.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@geond-agent/backend-adapter-sdk": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
