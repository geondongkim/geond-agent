{
  "name": "@geond-agent/claude-code-bridge",
  "version": "0.0.0",
  "private": true,
  "description": "Claude Code ACP bridge experiments for geond-agent.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@geond-agent/ui-workbench": "workspace:*",
    "@geond-agent/zai-provider": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
